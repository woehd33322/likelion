<center>
<div>
  <h3>멋쟁이 사자처럼 멤버 추가</h3>
  <%= form_for @user, :url => url_for(:controller => 'team', :action => 'create') do |f| %>
    <div class='input-div'>
      이름: <%= f.text_field :name, size: 20, :class => 'input-text' %>
    </div>
    <div class='input-div'>
      학과: <%= f.text_field :department, size: 20, :class => 'input-text' %>
    </div>
    <div class=''>
      <%= f.submit '추가', :class => 'input-button' %>
    </div>
  <% end %>
</div>
</center>

<center>
<div>
  <h3>멋쟁이 사자처럼 멤버 리스트</h3>
  <% @users.each do |user| %>
    <div>
      <p>
        <%= user.name %> & <%= user.department %> |
        <a href='/team/edit/<%= user.id %>'>수정</a> <a href='/team/delete/<%= user.id %>'>삭제</a>
      </p>
    </div>
  <% end %>
</div>
</center>